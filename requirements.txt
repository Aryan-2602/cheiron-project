# Lower bounds reflect the versions this was developed and verified against.
# Bounds rather than exact pins: there is no lockfile here, and hard pins in a
# reviewer-facing project fail a fresh install more often than they help.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0
openai>=2.0
httpx>=0.27

# Test-only
pytest>=8.0
pytest-asyncio>=0.24
respx>=0.21
